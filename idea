(1) an advanced/automatical programming language: e.g. just need to write down the function name, 
and then match such function name in, e.g. github, and find several candidates. 
In fact, what a programmer needs to do is to draw/illustrate the workflow. 
Such language is cross multiple existing languages. 
Based on the match, a program can be constituted by multiple languages (optimized somehow). 

(2) virtualize uCPE connected nodes and make it container-based
